---
interface Props {
  location: string;
}

const { location } = Astro.props;

const isEmpty = location === "No Location";
---

<span
  class={"location-chip" + (isEmpty ? " location-chip--empty" : "")}
  title={location}
>
  {location}
</span>
