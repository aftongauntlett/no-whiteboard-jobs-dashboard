---
interface Props {
  location: string;
}

const { location } = Astro.props;

const muted = location === "Not Available";
---

<span
  class={`location-chip ${muted ? "text-text-mutedLight dark:text-text-mutedDark" : ""}`}
>
  {location}
</span>
